% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_build_goal_events.R
\name{fbref_goal_events_scored}
\alias{fbref_goal_events_scored}
\title{Shared FBref scoring step: normalizes club names, parses the minute, and
derives \code{score_before_for}/\code{score_before_against} from the report's
running score (literal home:away, converted to for/against via \code{venue}).
Used by both \code{build_goal_events()} (Shearer, + Transfermarkt/matches joins)
and \code{build_comparator_goal_events()} (Phase 7 historical comparators, no
joins).}
\usage{
fbref_goal_events_scored(fbref_goal_events, cfg)
}
\arguments{
\item{fbref_goal_events}{Output of \code{ingest_fbref_goal_events()}.}

\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}
}
\value{
A tibble, one row per goal (own goals excluded).
}
\description{
Shared FBref scoring step: normalizes club names, parses the minute, and
derives \code{score_before_for}/\code{score_before_against} from the report's
running score (literal home:away, converted to for/against via \code{venue}).
Used by both \code{build_goal_events()} (Shearer, + Transfermarkt/matches joins)
and \code{build_comparator_goal_events()} (Phase 7 historical comparators, no
joins).
}
