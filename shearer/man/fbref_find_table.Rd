% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04c_ingest_fbref.R
\name{fbref_find_table}
\alias{fbref_find_table}
\title{A table FBref hides inside an HTML comment (player pages do this for
several tables, including stats_standard_dom_lg) has to be extracted as
comment text and re-parsed as its own document before \code{html_elements()}
can see inside it -- it is invisible to a normal query on \code{doc}.}
\usage{
fbref_find_table(doc, table_id)
}
\arguments{
\item{doc}{A parsed \code{xml_document} (from \code{read_html()}).}

\item{table_id}{The table's \code{id} attribute, e.g. "stats_standard_dom_lg".}
}
\value{
An \code{xml_node} for the \verb{<table>}, or \code{NULL} if not found (visible or hidden).
}
\description{
A table FBref hides inside an HTML comment (player pages do this for
several tables, including stats_standard_dom_lg) has to be extracted as
comment text and re-parsed as its own document before \code{html_elements()}
can see inside it -- it is invisible to a normal query on \code{doc}.
}
