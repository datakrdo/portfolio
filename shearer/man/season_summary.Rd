% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_analyse.R
\name{season_summary}
\alias{season_summary}
\title{Goals per season and club, plus minutes played and goals/90 -- FBref is
the only source with real per-match minutes, so \code{fbref_player_seasons}
(Shearer's own rows) supplies them here rather than deriving minutes from
appearances, which would assume every match was a full 90.}
\usage{
season_summary(goal_events, fbref_player_seasons, cfg)
}
\arguments{
\item{goal_events}{The \code{shearer_goal_events} tibble (goal_events_checked in \verb{_targets.R}).}

\item{fbref_player_seasons}{Output of \code{ingest_fbref_player_seasons()}.}

\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}
}
\value{
See description above.
}
\description{
Goals per season and club, plus minutes played and goals/90 -- FBref is
the only source with real per-match minutes, so \code{fbref_player_seasons}
(Shearer's own rows) supplies them here rather than deriving minutes from
appearances, which would assume every match was a full 90.
}
