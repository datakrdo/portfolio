% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_shared.R
\name{plot_series}
\alias{plot_series}
\title{Line-and-point time series with a plotly tooltip column -- the shape shared
by every season-indexed line plot in app/app.R (contemporary comparison,
league scoring trend). x/y/tip are embraced ({{ }}); colour/group are
optional and forwarded as quosures via rlang::enquo()/quo_is_null(), so a
caller can omit them (single series, solid \code{line_colour}) or map a column
\emph{or} a literal (e.g. group = 1) exactly as they would write aes() by hand.
\code{line_colour} is ignored once \code{colour} is mapped -- a fixed and a mapped
colour on the same aesthetic can't coexist.}
\usage{
plot_series(
  data,
  x,
  y,
  tip,
  colour = NULL,
  group = NULL,
  line_colour = SHEARER_COLOUR
)
}
\arguments{
\item{data}{A data frame; \code{x}/\code{y}/\code{tip} columns are evaluated in its scope.}

\item{x}{Bare column for the x aesthetic; embraced with \code{{{ }}}.}

\item{y}{Bare column for the y aesthetic; embraced with \code{{{ }}}.}

\item{tip}{Bare column (or expression) supplying the plotly tooltip text; embraced with \code{{{ }}}.}

\item{colour}{Bare column for the \code{colour} aesthetic; embraced with \code{{{ }}}. Omit for a single-colour plot.}

\item{group}{Bare column (or literal, e.g. \code{1}) for the \code{group} aesthetic; embraced with \code{{{ }}}.}

\item{line_colour}{Fixed line/point colour used when \code{colour} is not mapped.}
}
\value{
A ggplot object.
}
\description{
Line-and-point time series with a plotly tooltip column -- the shape shared
by every season-indexed line plot in app/app.R (contemporary comparison,
league scoring trend). x/y/tip are embraced ({{ }}); colour/group are
optional and forwarded as quosures via rlang::enquo()/quo_is_null(), so a
caller can omit them (single series, solid \code{line_colour}) or map a column
\emph{or} a literal (e.g. group = 1) exactly as they would write aes() by hand.
\code{line_colour} is ignored once \code{colour} is mapped -- a fixed and a mapped
colour on the same aesthetic can't coexist.
}
