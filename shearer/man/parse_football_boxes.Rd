% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_ingest_wikipedia.R
\name{parse_football_boxes}
\alias{parse_football_boxes}
\title{Parse a \verb{\{\{football box collapsible\}\}} run into the same
date / opponent / venue / result / scorers_raw shape the wikitable parser
returns. Each template is a flat list of \verb{|name = value} lines; only
date, team1, team2, score and goals1/goals2 are needed.}
\usage{
parse_football_boxes(section, club, cfg)
}
\arguments{
\item{section}{A wikitext section (e.g. from \code{extract_pl_section()}).}

\item{club}{Canonical club name (as used in config.yaml's \code{club_name_lookup}).}

\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}
}
\value{
A tibble.
}
\description{
\code{club} is the canonical name of the club whose article this is, used to
decide which side of each fixture is "us" -- the templates are written
home-team-first regardless of who the article is about.
}
