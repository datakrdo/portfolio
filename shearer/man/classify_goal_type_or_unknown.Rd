% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_build_goal_events.R
\name{classify_goal_type_or_unknown}
\alias{classify_goal_type_or_unknown}
\title{\code{classify_goal_type()}, but for a goal that has no Transfermarkt match at
all (NA \code{goal_type}, e.g. no \verb{(season, opponent, minute)} hit) -- reported
as "unknown" instead of erroring, since a missing cross-check row is a
declared coverage gap, not an unclassified value.}
\usage{
classify_goal_type_or_unknown(goal_type, taxonomy)
}
\arguments{
\item{goal_type}{Free-text "Type of goal" value, or NA if unmatched.}

\item{taxonomy}{The \code{goal_type_taxonomy} list from config.yaml.}
}
\value{
See description above.
}
\description{
\code{classify_goal_type()}, but for a goal that has no Transfermarkt match at
all (NA \code{goal_type}, e.g. no \verb{(season, opponent, minute)} hit) -- reported
as "unknown" instead of erroring, since a missing cross-check row is a
declared coverage gap, not an unclassified value.
}
