% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_ingest_wikipedia.R
\name{ingest_season_top_scorers}
\alias{ingest_season_top_scorers}
\title{Per-season top-10 scorer table -- the real competitive field each season,
used for contemporary comparators. Not a curated shortlist of players.}
\usage{
ingest_season_top_scorers(cfg)
}
\arguments{
\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}
}
\value{
A tibble.
}
\description{
Same one-cell-per-line style as \code{ingest_all_time_scorers()}, plus
\code{rowspan=N} continuation rows for tied ranks -- but Wikipedia is not
consistent about \emph{what} gets carried by rowspan: some seasons repeat each
tied player's goal tally on its own row (continuation row = 3 cells:
player/club/goals, only rank carried), others also rowspan the goals
column itself (continuation row = 2 cells: player/club, both rank and
goals carried). Rather than assume one shape, classify each cell as
numeric (rank or goals) or text (player or club) and infer the shape from
that, carrying forward whichever of rank/goals a row omits.
}
