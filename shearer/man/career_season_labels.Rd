% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_ingest_wikipedia.R
\name{career_season_labels}
\alias{career_season_labels}
\title{The 14 career seasons as the two-digit labels every downstream table uses.}
\usage{
career_season_labels(cfg)
}
\arguments{
\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}
}
\value{
See description above.
}
\description{
config.yaml stores them in Wikipedia's article-title convention
("1999-2000") because that is what the fetch URLs need. Anything that
\emph{compares} or \emph{joins} on a season must go through here instead of reading
the config list directly -- otherwise 1999-2000 is the one season that
silently fails to match and disappears from the result.
}
