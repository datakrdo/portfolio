% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_ingest_wikipedia.R
\name{parse_season_pl_results}
\alias{parse_season_pl_results}
\title{Parse whichever of the two league-results formats this season's article
uses, returning one row per match with Shearer's goals counted -- or zero
rows for a season the article does not cover match by match, which the
caller declares as a coverage gap rather than filling in.}
\usage{
parse_season_pl_results(wikitext, club, cfg)
}
\arguments{
\item{wikitext}{Raw wikitext of a Wikipedia article (or section).}

\item{club}{Canonical club name (as used in config.yaml's \code{club_name_lookup}).}

\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}
}
\value{
A tibble.
}
\description{
Parse whichever of the two league-results formats this season's article
uses, returning one row per match with Shearer's goals counted -- or zero
rows for a season the article does not cover match by match, which the
caller declares as a coverage gap rather than filling in.
}
