% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_sources.R
\name{cache_fetch_chromote}
\alias{cache_fetch_chromote}
\title{Live fetch through a headless, real Chrome (\code{chromote}), for the small,
confirmed subset of fbref.com pages the Wayback Machine never captured at
all (checked via \code{wayback_snapshot()} returning \code{NA} with no CDX rows under
any status code -- not a transient miss). fbref.com issues a Cloudflare
Managed Challenge on a plain \code{httr2} request; a real browser clears it.
One \code{ChromoteSession} is started lazily and reused across calls in a run
(starting Chrome per-call would be ~28x slower for no benefit).}
\usage{
cache_fetch_chromote(url, dest, source_name, cfg, notes = NA_character_)
}
\arguments{
\item{url}{The live fbref.com URL to fetch.}

\item{dest}{Path (relative to project root) to write the rendered HTML to.}

\item{source_name}{Short label matching a \verb{sources:} entry in config/config.yaml.}

\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}

\item{notes}{Optional free-text note for the manifest row.}
}
\value{
\code{dest}, invisibly.
}
\description{
Live fetch through a headless, real Chrome (\code{chromote}), for the small,
confirmed subset of fbref.com pages the Wayback Machine never captured at
all (checked via \code{wayback_snapshot()} returning \code{NA} with no CDX rows under
any status code -- not a transient miss). fbref.com issues a Cloudflare
Managed Challenge on a plain \code{httr2} request; a real browser clears it.
One \code{ChromoteSession} is started lazily and reused across calls in a run
(starting Chrome per-call would be ~28x slower for no benefit).
}
