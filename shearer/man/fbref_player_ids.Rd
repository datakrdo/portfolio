% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04c_ingest_fbref.R
\name{fbref_player_ids}
\alias{fbref_player_ids}
\title{FBref player ids for the historical comparators plus Shearer, pinned in
\code{config.yaml}'s \code{sources.fbref.comparator_player_ids} -- resolved once
(2026-09-20) against an archived Premier-League season-stats page, which
lists every that-season player's \verb{/en/players/\{id\}/\{name\}} link. Returned
from config rather than re-parsed on every run: the ids don't change, and
re-deriving them would add a fragile network dependency for no benefit.}
\usage{
fbref_player_ids(cfg)
}
\arguments{
\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}
}
\value{
A named character vector, player name -> FBref id.
}
\description{
FBref player ids for the historical comparators plus Shearer, pinned in
\code{config.yaml}'s \code{sources.fbref.comparator_player_ids} -- resolved once
(2026-09-20) against an archived Premier-League season-stats page, which
lists every that-season player's \verb{/en/players/\{id\}/\{name\}} link. Returned
from config rather than re-parsed on every run: the ids don't change, and
re-deriving them would add a fragile network dependency for no benefit.
}
