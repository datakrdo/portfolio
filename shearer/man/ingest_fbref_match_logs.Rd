% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04c_ingest_fbref.R
\name{ingest_fbref_match_logs}
\alias{ingest_fbref_match_logs}
\title{Match logs for all PL seasons of a player's career, one row per match (PL
and every other competition -- callers filter to Premier League). This is
the backbone \code{build_goal_events()} (Shearer) and
\code{comparator_context_breakdown_for()} (Phase 7 historical comparators)
build from: season/opponent/venue/result/minutes/goals per match.}
\usage{
ingest_fbref_match_logs(
  cfg,
  player_id = cfg$sources$fbref$player_id,
  player_name = cfg$project$player_name,
  seasons = cfg$sources$fbref$seasons
)
}
\arguments{
\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}

\item{player_id}{FBref player id. Defaults to Shearer's.}

\item{player_name}{Player's full name (used to build the URL slug). Defaults to Shearer's.}

\item{seasons}{Character vector of FBref season labels (e.g. "1996-1997") to fetch.
Defaults to Shearer's full career (\code{cfg$sources$fbref$seasons}).}
}
\value{
A tibble.
}
\description{
Defaults reproduce the original Shearer-only call exactly, including the
cache filename (\verb{fbref_matchlog_\{season\}.html}, no player infix) -- so the
~14 already-cached fetches from before this function took a player
argument aren't invalidated. A comparator's \code{player_id} differs from the
default, so its files get a player-id infix instead, keeping the ten
players' caches from colliding with each other.
}
