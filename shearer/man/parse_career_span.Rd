% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_ingest_wikipedia.R
\name{parse_career_span}
\alias{parse_career_span}
\title{The First/Last Premier League year pair, written as one cell holding two
values joined by "||" ("1992||2006") because the table's other cells are
one-per-line. \code{idx} differs by one depending on whether the row carried its
own goals cell or inherited it via rowspan, so the caller passes it in.
Returns NA/NA rather than erroring -- a comparator with no parseable span is
excluded from the era adjustment, not guessed at (see R/10).}
\usage{
parse_career_span(cells, idx)
}
\arguments{
\item{cells}{Character vector of a wikitable row's pipe-delimited cells.}

\item{idx}{Index of the goals cell within \code{cells} (varies with rowspan continuation).}
}
\value{
A tibble.
}
\description{
The First/Last Premier League year pair, written as one cell holding two
values joined by "||" ("1992||2006") because the table's other cells are
one-per-line. \code{idx} differs by one depending on whether the row carried its
own goals cell or inherited it via rowspan, so the caller passes it in.
Returns NA/NA rather than erroring -- a comparator with no parseable span is
excluded from the era adjustment, not guessed at (see R/10).
}
