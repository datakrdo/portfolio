% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04c_ingest_fbref.R
\name{ingest_fbref_goal_events}
\alias{ingest_fbref_goal_events}
\title{One event per goal, parsed from each scoring match's report page
(\verb{#events_wrap div.event}). Only fetches reports for matches with
\code{goals > 0} in \code{logs} -- matches with no goal from \code{player_name} have
nothing a report would add. Match reports are cached by match slug, not by
player, so two comparators who both scored in the same historical match
share one fetch.}
\usage{
ingest_fbref_goal_events(cfg, logs, player_name = cfg$project$player_name)
}
\arguments{
\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}

\item{logs}{Output of \code{ingest_fbref_match_logs()}, filtered to Premier League.}

\item{player_name}{Player's full name, matched against the report's scorer
text. Defaults to Shearer's.}
}
\value{
A tibble, one row per goal, with a \code{coverage_gaps} attribute
(character vector naming any match report the Archive had no capture of).
}
\description{
One event per goal, parsed from each scoring match's report page
(\verb{#events_wrap div.event}). Only fetches reports for matches with
\code{goals > 0} in \code{logs} -- matches with no goal from \code{player_name} have
nothing a report would add. Match reports are cached by match slug, not by
player, so two comparators who both scored in the same historical match
share one fetch.
}
