% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_sources.R
\name{cache_fetch_json}
\alias{cache_fetch_json}
\title{Fetch and parse a JSON API response (used for the Wikipedia API), caching
the raw JSON the same way \code{cache_fetch()} caches HTML/CSV.}
\usage{
cache_fetch_json(url, dest, source_name, cfg, notes = NA_character_)
}
\arguments{
\item{url}{Full URL to fetch.}

\item{dest}{Path (relative to project root) to write the raw response to.}

\item{source_name}{Short label matching a \verb{sources:} entry in config/config.yaml, e.g. "transfermarkt_goal_log".}

\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}

\item{notes}{Optional free-text note for the manifest row.}
}
\value{
See description above.
}
\description{
Fetch and parse a JSON API response (used for the Wikipedia API), caching
the raw JSON the same way \code{cache_fetch()} caches HTML/CSV.
}
