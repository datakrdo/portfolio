% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_validate.R
\name{validate_goal_events}
\alias{validate_goal_events}
\title{Run every sec. 14 check against shearer_goal_events. Returns the pointblank
agent (for docs/validation.md) and stops the pipeline via
pointblank::stop_if_not() semantics if any check fails.}
\usage{
validate_goal_events(
  goal_events,
  cfg,
  crosscheck = NULL,
  span_mismatches = NULL
)
}
\arguments{
\item{goal_events}{The \code{shearer_goal_events} tibble (goal_events_checked in \verb{_targets.R}).}

\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}

\item{crosscheck}{Output of \code{crosscheck_goal_counts()}.}

\item{span_mismatches}{Output of \code{historical_table_span_mismatches()}.}
}
\value{
The validated input, returned unchanged (so this doubles as a pass-through gate).
}
\description{
Run every sec. 14 check against shearer_goal_events. Returns the pointblank
agent (for docs/validation.md) and stops the pipeline via
pointblank::stop_if_not() semantics if any check fails.
}
