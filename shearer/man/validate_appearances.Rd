% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_validate.R
\name{validate_appearances}
\alias{validate_appearances}
\title{Cross-check the goal log (FBref, via goal_events) against Shearer's own Wikipedia
infobox -- an independent source with no shared parsing path.}
\usage{
validate_appearances(shearer_appearances, season_summary, cfg)
}
\arguments{
\item{shearer_appearances}{Output of \code{ingest_shearer_appearances()} / \code{validate_appearances()}.}

\item{season_summary}{Output of \code{season_summary()} -- one row per season/club.}

\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}
}
\value{
The validated input, returned unchanged (so this doubles as a pass-through gate).
}
\description{
The season-\emph{count} assertion is the one that matters most: a per-season
value comparison alone cannot catch a silently dropped season, because a
missing row has nothing to disagree with. 1999-2000 went missing exactly
this way (its config label uses Wikipedia's article convention), which is
why the expected season list comes from career_season_labels().
}
