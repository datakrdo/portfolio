% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_build_goal_events.R
\name{build_comparator_goal_events}
\alias{build_comparator_goal_events}
\title{Lighter version of \code{build_goal_events()} for the historical comparators
(Phase 7): only \code{score_before_for}/\code{score_before_against} and \code{minute} are
needed to feed \code{classify_goal_context()} -- no Transfermarkt \code{goal_type}
join (not fetched for comparators) and no \code{matches} join (comparators have
no football-data.co.uk fixture list, so no \code{home_goals}/\code{away_goals};
\code{classify_goal_context()} tolerates their absence).}
\usage{
build_comparator_goal_events(cfg, fbref_goal_events)
}
\arguments{
\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}

\item{fbref_goal_events}{Output of \code{ingest_fbref_goal_events()} for one comparator.}
}
\value{
A tibble, one row per goal.
}
\description{
Lighter version of \code{build_goal_events()} for the historical comparators
(Phase 7): only \code{score_before_for}/\code{score_before_against} and \code{minute} are
needed to feed \code{classify_goal_context()} -- no Transfermarkt \code{goal_type}
join (not fetched for comparators) and no \code{matches} join (comparators have
no football-data.co.uk fixture list, so no \code{home_goals}/\code{away_goals};
\code{classify_goal_context()} tolerates their absence).
}
