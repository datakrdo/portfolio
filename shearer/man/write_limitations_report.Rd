% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12_reports.R
\name{write_limitations_report}
\alias{write_limitations_report}
\title{Known limitations, each tied to a cfg value so the numbers referenced here
can't silently drift from what the rest of the pipeline actually uses.}
\usage{
write_limitations_report(cfg, path)
}
\arguments{
\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}

\item{path}{Output file path.}
}
\value{
The \code{path} written to, invisibly usable for chaining into \verb{_targets.R}.
}
\description{
Known limitations, each tied to a cfg value so the numbers referenced here
can't silently drift from what the rest of the pipeline actually uses.
}
