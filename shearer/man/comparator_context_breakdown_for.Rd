% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_analyse.R
\name{comparator_context_breakdown_for}
\alias{comparator_context_breakdown_for}
\title{Full clutch-context breakdown for one historical comparator (Phase 7):
fetches that player's own PL match logs and match reports (independently
of Shearer's -- \code{cache_fetch_wayback()} skips files that already exist, so
this is resumable), scores and classifies them the same way Shearer's own
branch does, and reports the resulting context share. Never touches
Shearer's own targets/cache files.}
\usage{
comparator_context_breakdown_for(player, player_id, cfg, fbref_player_seasons)
}
\arguments{
\item{player}{Player name, matching a key in \code{fbref_player_ids(cfg)}.}

\item{player_id}{FBref id for \code{player}.}

\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}

\item{fbref_player_seasons}{Output of \code{ingest_fbref_player_seasons()} --
supplies \code{player}'s own Premier League seasons, so they aren't hardcoded here.}
}
\value{
A \code{context_breakdown()}-shaped tibble tagged with a \code{player} column,
plus a \code{coverage_gaps} attribute (character vector of match reports the
Archive had no capture of).
}
\description{
Full clutch-context breakdown for one historical comparator (Phase 7):
fetches that player's own PL match logs and match reports (independently
of Shearer's -- \code{cache_fetch_wayback()} skips files that already exist, so
this is resumable), scores and classifies them the same way Shearer's own
branch does, and reports the resulting context share. Never touches
Shearer's own targets/cache files.
}
