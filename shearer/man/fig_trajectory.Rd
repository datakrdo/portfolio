% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_figures.R
\name{fig_trajectory}
\alias{fig_trajectory}
\title{Goals per season, coloured by club (in \code{cfg$project$clubs} order -- the
first club gets the accent colour, every other club is neutral), with
each club's peak season labelled.}
\usage{
fig_trajectory(season_summary, cfg)
}
\arguments{
\item{season_summary}{Output of \code{season_summary()} -- one row per season/club.}

\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}
}
\value{
A ggplot (or gt table) object.
}
\description{
Goals per season, coloured by club (in \code{cfg$project$clubs} order -- the
first club gets the accent colour, every other club is neutral), with
each club's peak season labelled.
}
