% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_sources.R
\name{wayback_snapshot}
\alias{wayback_snapshot}
\title{Resolve the Wayback Machine snapshot URL for \code{original_url} via the CDX
API, picking the snapshot with the largest \code{length} (the full-page
capture: FBref's \code{id_} raw mode was observed returning a truncated,
table-less 47 KB snapshot next to a 222 KB normal-mode one for the same
URL, so "biggest" is a real signal, not a tie-break).}
\usage{
wayback_snapshot(original_url, cfg, exclude_timestamps = character(0))
}
\arguments{
\item{original_url}{The live URL to look up (e.g. an fbref.com page).}

\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}

\item{exclude_timestamps}{Character vector of snapshot timestamps already tried.}
}
\value{
A \verb{https://web.archive.org/web/\{timestamp\}/\{original_url\}} string, or \code{NA} if none left.
}
\description{
Some CDX entries turn out to 404 when actually fetched (a stale index,
not a real absence) -- \code{exclude_timestamps} lets \code{cache_fetch_wayback()}
ask for the next-largest snapshot instead of giving up after one miss.
}
