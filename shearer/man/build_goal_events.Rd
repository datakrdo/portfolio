% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_build_goal_events.R
\name{build_goal_events}
\alias{build_goal_events}
\title{Build shearer_goal_events from FBref's match logs + match reports, then
left-joins Transfermarkt's goal log for \code{goal_type} only (by season,
opponent, minute -- a goal not matched there is reported as "unknown",
not guessed at). Joins to \code{matches} via (season, venue, opponent), the
same key \code{matches$match_id} is built from.}
\usage{
build_goal_events(cfg, matches, fbref_goal_events)
}
\arguments{
\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}

\item{matches}{The \code{matches} tibble (one row per fixture).}

\item{fbref_goal_events}{Output of \code{ingest_fbref_goal_events()} (a \verb{_targets.R}
target of its own, so the ~185 match-report fetches are cached independently
of this function).}
}
\value{
A tibble.
}
\description{
Build shearer_goal_events from FBref's match logs + match reports, then
left-joins Transfermarkt's goal log for \code{goal_type} only (by season,
opponent, minute -- a goal not matched there is reported as "unknown",
not guessed at). Joins to \code{matches} via (season, venue, opponent), the
same key \code{matches$match_id} is built from.
}
