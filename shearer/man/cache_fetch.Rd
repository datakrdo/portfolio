% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_sources.R
\name{cache_fetch}
\alias{cache_fetch}
\title{Fetch a URL exactly once and cache it under data/raw/.}
\usage{
cache_fetch(url, dest, source_name, cfg, notes = NA_character_)
}
\arguments{
\item{url}{Full URL to fetch.}

\item{dest}{Path (relative to project root) to write the raw response to,
e.g. "data/raw/transfermarkt_shearer_goals.html".}

\item{source_name}{Short label matching a \verb{sources:} entry in
config/config.yaml, e.g. "transfermarkt_goal_log".}

\item{cfg}{The parsed config list (from \code{load_config()}), used for the
user agent and rate-limit pause.}

\item{notes}{Optional free-text note for the manifest row (e.g. season).}
}
\value{
\code{dest}, invisibly.
}
\description{
If \code{dest} already exists, the network is never touched and no new
manifest row is written -- the existing row is the provenance record.
On a real fetch, appends one row to \code{manifest_path} with the retrieval
timestamp, HTTP status, and byte count.
}
