% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_ingest_wikipedia.R
\name{split_wikitable_cells}
\alias{split_wikitable_cells}
\title{Split a "|-"-delimited wikitable row into its one-cell-per-line cells.
Some rows write a cell with no attributes as "||value" (an empty
attribute string before the real cell-start "|") instead of "|value" --
collapsing that quirk to a single "|" first means every row's first real
delimiter lines up, so the leading empty piece dropped by \verb{[-1]} is
always actually empty, never a swallowed cell.}
\usage{
split_wikitable_cells(row)
}
\arguments{
\item{row}{A single \verb{\{\{football box collapsible\}\}} template's lines.}
}
\value{
See description above.
}
\description{
Split a "|-"-delimited wikitable row into its one-cell-per-line cells.
Some rows write a cell with no attributes as "||value" (an empty
attribute string before the real cell-start "|") instead of "|value" --
collapsing that quirk to a single "|" first means every row's first real
delimiter lines up, so the leading empty piece dropped by \verb{[-1]} is
always actually empty, never a swallowed cell.
}
