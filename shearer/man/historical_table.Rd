% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_analyse_comparators.R
\name{historical_table}
\alias{historical_table}
\title{All-time 100+ list with configured players flagged, goals/appearance,
goals/90 and the era-adjusted rate for both.}
\usage{
historical_table(
  all_time_scorers,
  cfg,
  league_scoring_trend,
  fbref_player_seasons
)
}
\arguments{
\item{all_time_scorers}{Output of \code{ingest_all_time_scorers()}.}

\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}

\item{league_scoring_trend}{Output of \code{league_scoring_trend()} -- one row per season.}

\item{fbref_player_seasons}{Output of \code{ingest_fbref_player_seasons()}.}
}
\value{
See description above.
}
\description{
\code{goals_per_90} needs real minutes, which only FBref has -- \code{fbref_player_seasons}
is pinned to the players in \code{config.yaml}'s \code{sources.fbref.comparator_player_ids}
(resolved via \code{fbref_player_ids()}). A player outside that pinned set gets
\code{NA} for \code{goals_per_90}/\code{adjusted_goals_per_90} -- a declared coverage gap,
not a guess -- while \code{goals_per_appearance} (Wikipedia-sourced, covers the
whole all-time list) stays populated for everyone.
}
\details{
\code{goals_per_90}'s numerator (Wikipedia's career total) and denominator
(FBref's summed minutes) only agree when the cached FBref page happens to
cover the player's whole career. When it doesn't (an archived snapshot that
predates a player's later seasons), the sum of FBref's own per-season goals
falls short of the Wikipedia total, and dividing the full total by partial
minutes silently inflates the rate. \code{spans_agree} catches that case and
forces \code{NA} instead -- a second, distinct reason to be missing goals_per_90,
alongside "not in the pinned comparator set" above.

Raw goals and raw rates are always kept alongside the adjusted figures --
the adjustment is offered as a second reading, never as a replacement for
what the sources actually recorded.
}
