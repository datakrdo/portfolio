% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_sources.R
\name{cache_fetch_wayback}
\alias{cache_fetch_wayback}
\title{\code{cache_fetch()}, but resolved through the Wayback Machine: the manifest
records the \emph{original} FBref URL (provenance points at the source, the
Archive is only the transport), and the CDX lookup plus fetch both retry
with backoff on 429/503 -- the Archive throws those often under load.}
\usage{
cache_fetch_wayback(
  original_url,
  dest,
  source_name,
  cfg,
  notes = NA_character_
)
}
\arguments{
\item{original_url}{The live FBref URL this snapshot stands in for.}

\item{dest}{Path (relative to project root) to write the raw response to.}

\item{source_name}{Short label matching a \verb{sources:} entry in config/config.yaml.}

\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}

\item{notes}{Optional free-text note for the manifest row.}
}
\value{
\code{dest}, invisibly.
}
\description{
\code{cache_fetch()}, but resolved through the Wayback Machine: the manifest
records the \emph{original} FBref URL (provenance points at the source, the
Archive is only the transport), and the CDX lookup plus fetch both retry
with backoff on 429/503 -- the Archive throws those often under load.
}
