% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_sources.R
\name{strip_wayback_toolbar}
\alias{strip_wayback_toolbar}
\title{Strip the Wayback Machine's injected toolbar (\verb{#wm-ipp-base} and its
\verb{<script>}/\verb{<style>} siblings) out of a cached snapshot in place, so
downstream \code{read_html()} calls see the same markup the original page had.}
\usage{
strip_wayback_toolbar(path)
}
\arguments{
\item{path}{Path to a cached HTML file (as written by \code{cache_fetch_wayback()}).}
}
\value{
\code{path}, invisibly.
}
\description{
Strip the Wayback Machine's injected toolbar (\verb{#wm-ipp-base} and its
\verb{<script>}/\verb{<style>} siblings) out of a cached snapshot in place, so
downstream \code{read_html()} calls see the same markup the original page had.
}
