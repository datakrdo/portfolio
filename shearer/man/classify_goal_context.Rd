% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_classify_context.R
\name{classify_goal_context}
\alias{classify_goal_context}
\title{Classify each goal's context from the score immediately before it. A goal
always moves the scorer's margin by exactly +1, so "ahead" is only
reachable from level (0 -> 1), never directly from behind (-1 -> 0 is a
tie, not a lead):
equaliser       team was 1 down, goal ties it (margin_before == -1)
go_ahead        team was level, goal puts them ahead (margin_before == 0)
reduce_deficit  team still behind after the goal (margin_before < -1)
extend_lead     team already ahead before the goal (margin_before > 0)
Also flags late_goal (>= late_goal_minute) and a close-match state
(margin before the goal <= close_margin_abs).}
\usage{
classify_goal_context(goal_events, cfg)
}
\arguments{
\item{goal_events}{The \code{shearer_goal_events} tibble (goal_events_checked in \verb{_targets.R}),
or the lighter comparator tibble from \code{build_comparator_goal_events()}
(Phase 7), which has no \code{home_goals}/\code{away_goals} (no fixture list for
comparators) -- \code{final_margin_le_1} is \code{NA} in that case, a declared gap
rather than a guess.}

\item{cfg}{Parsed config list (from \code{load_config()} / config/config.yaml).}
}
\value{
See description above.
}
\description{
Classify each goal's context from the score immediately before it. A goal
always moves the scorer's margin by exactly +1, so "ahead" is only
reachable from level (0 -> 1), never directly from behind (-1 -> 0 is a
tie, not a lead):
equaliser       team was 1 down, goal ties it (margin_before == -1)
go_ahead        team was level, goal puts them ahead (margin_before == 0)
reduce_deficit  team still behind after the goal (margin_before < -1)
extend_lead     team already ahead before the goal (margin_before > 0)
Also flags late_goal (>= late_goal_minute) and a close-match state
(margin before the goal <= close_margin_abs).
}
