[project]
name = "vigilancia-ar"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "dbt-databricks>=1.12.5",
]

[dependency-groups]
dev = [
    "sqlfluff>=3.0.0",
    "sqlfluff-templater-dbt>=3.0.0",
]
